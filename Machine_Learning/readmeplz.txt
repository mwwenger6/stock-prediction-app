will need to make sure you have numpy, pandas, 
matplotlib, keras, TensorFlow installed

use the console command: python -m pip install package_name

Also want to make sure python is in your PATH

Note that for TensorFlow, your Python virgin needs to be 3.11 or lower since
TensorFlow does not currently support 3.12