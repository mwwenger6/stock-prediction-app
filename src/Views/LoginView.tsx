import {Container} from "react-bootstrap";

const LoginView = () => {

    return (
        <Container>
            <div>
                This will be the login page
            </div>
        </Container>
    );
}

export default LoginView;